/**
 * Canonical GitHub repo for About commit links, version metadata, and fallbacks.
 * Keep the string in sync with `APP_REPO_URL` in `vite.config.ts`.
 */
export const APP_REPO_URL = 'https://github.com/gingerbreap/tpghelper'
